---
const {
  text = "Título!",
  message = "Mensaje predeterminado",
  icon = "/public/icons/hamburger-svgrepo-com.svg",
  extraClass = "",
  extraClassText = "",
} = Astro.props;
---

<div
  class={`p-6 max-w-sm mx-auto bg-white rounded-xl shadow-lg flex items-center gap-x-4 ${extraClass}`}
>
  <div class="shrink-0">
    <img class="w-12 h-12" src={icon} alt="Icono" />
  </div>
  <div class={`flex flex-col ${extraClassText}`}>
    <div class="text-xl font-medium text-black">{text}</div>
    <p class="text-slate-500 mb-2">{message}</p>
  </div>
</div>
